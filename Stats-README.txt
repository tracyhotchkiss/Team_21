

to start a local server , move to the directory in cmd or terminal

and type 

python3 -m http.server 9000

the 9000 is optional will default to 8000

then go to the browser and type in---> http://localhost:8000/

then open the main page the rest should be linked :)